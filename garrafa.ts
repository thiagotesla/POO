class Garrafa {
    altura: number;
    largura: number;
    capacidade: number;

    constructor(
        altura: number,
        largura: number,
        capacidade: number,
    ) {
        this.altura = altura;
        this.largura = largura;
        this.capacidade = capacidade;
    }

    abrir() { }
    fechar() { }
}